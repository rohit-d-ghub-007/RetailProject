pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "build has completed successfully"
            }
        }

        stage('Test') {
            steps {
                echo "test has completed successfully"
            }
        }

        stage('Package') {
            steps {
                echo "package has completed successfully"
            }
        }

        stage('Deploy') {
            steps {
                echo "deploy has completed successfully"
            }
        }
    }
}

